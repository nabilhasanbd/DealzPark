name: dealzpark_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0            # For making HTTP requests
  provider: ^6.1.2        # For state management
  intl: ^0.19.0           # For date formatting
  flutter_staggered_grid_view: ^0.7.0 # For a more dynamic grid layout (optional, but nice for deals)
  #cupertino_icons: ^1.0.2 # Already there
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0   # For linting

flutter:
  uses-material-design: true

  